#include <bits/stdc++.h>
using namespace std;


vector<int> adj[1'005];
int deg[1'005];
int n, m;


int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin>>n>>m;
    while(m--){
        int q;
        cin>>q;
        --q;
        /*큐를 두개 두어서, 하나는 앞에 놓고, 뒤쪽걸 빼서 앞에 넣고 하는 식으로*/
        while(q--){
            int u, v; 
        
        }
    }
}